apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: roboshop-dev
  region: us-east-1

managedNodeGroups:
  - name: roboshop-dev2
    instanceTypes:
      - m7i-flex.large
      - m7i-flex.large
    desiredCapacity: 2
    spot: true
